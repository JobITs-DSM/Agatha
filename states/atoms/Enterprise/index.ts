import { atom } from "recoil";

export const EnterpriseModal = atom({
  key: "enterprise-modal",
  default: false,
});

export const EnterpriseCheckBox = atom({
  key: "enterprise-checkbox",
  default: false,
});
